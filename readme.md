## Cool git commands!!

```bash
// add all of your files
git add --all

// commit your files!
git commit -m "some useful commit!"

// push them up
git push
```

Any trouble use:

```bash
git stash
```

Google can save your life!!